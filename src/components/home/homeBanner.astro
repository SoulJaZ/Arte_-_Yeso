---
import banner from "../../assets/img/headerImg2.jpg";
---

<section
  class="relative min-h-[60vh] md:min-h-[70vh] flex items-center text-white px-6 bg-cover bg-center"
  style={{ backgroundImage: `url(${banner.src})` }}
>
  <!-- Overlay -->
  <div class="absolute inset-0 bg-black/60"></div>

  <div
    class="relative max-w-3xl mx-auto flex flex-col items-center gap-6 text-center
           animate-fade-in-up"
  >
    <!-- Title -->
    <h2 class="text-2xl sm:text-4xl md:text-5xl font-bold leading-tight">
      Yesos, Drywall y Acabados Profesionales en Bogotá
    </h2>

    <!-- Description -->
    <p class="text-gray-200 text-lg sm:text-xl">
      Maestro de obra con más de 40 años de experiencia en instalación de yeso,
      drywall, cielos rasos, cornisas y acabados interiores.
      Servicio profesional en <strong>Suba y toda Bogotá</strong>, con calidad,
      precisión y cumplimiento garantizado.
    </p>

    <!-- Button -->
    <a
      href="/contact"
      aria-label="Solicitar cotización de yesos y drywall en Bogotá"
      class="bg-[#F9C74F] text-[#0B2545] hover:bg-[#f1b500] px-8 py-3 rounded-full font-bold transition hover:scale-105"
    >
      Solicitar cotización
    </a>
  </div>
</section>

<style>
@keyframes fade-in-up {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-in-up {
  animation: fade-in-up 0.9s ease-out both;
}
</style>
