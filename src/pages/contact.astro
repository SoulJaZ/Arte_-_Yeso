---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <section class="px-6 py-20 max-w-7xl mx-auto">
    <!-- Title -->
    <h2
      class="text-[#0B2545] text-3xl md:text-4xl font-extrabold text-center uppercase mb-12"
    >
      Contacto
    </h2>

    <div class="flex flex-col md:flex-row items-start gap-12">
      <!-- Text -->
      <div
        class="w-full md:w-1/2 text-center md:text-left flex flex-col justify-center"
      >
        <p class="text-gray-700 text-2xl font-bold leading-8 mb-6">
          ¿Tiene una idea, proyecto o negocio que quiere llevar al siguiente
          nivel?
        </p>

        <p class="text-gray-700 text-lg leading-7 mb-4">
          Déjeme un mensaje y me pondré en contacto con usted lo antes posible.
          Cada proyecto lo trabajo de forma personalizada.
        </p>

        <p class="text-gray-700 text-lg leading-7">
          También puede escribirme directamente por correo o teléfono si lo
          prefiere.
        </p>
      </div>

      <!-- Form -->
      <div class="w-full md:w-1/2 bg-gray-50 rounded-2xl p-8">
        <form
          action="https://formspree.io/f/mwvqrwwb"
          method="POST"
          class="space-y-6"
        >
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="flex flex-col">
              <label class="font-semibold text-[#0B2545] mb-2">
                Nombre
              </label>
              <input
                type="text"
                name="nombre"
                required
                placeholder="Su nombre"
                class="border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#F9C74F]"
              />
            </div>

            <div class="flex flex-col">
              <label class="font-semibold text-[#0B2545] mb-2">
                Apellido
              </label>
              <input
                type="text"
                name="apellido"
                required
                placeholder="Su apellido"
                class="border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#F9C74F]"
              />
            </div>

            <div class="flex flex-col">
              <label class="font-semibold text-[#0B2545] mb-2">
                Correo electrónico
              </label>
              <input
                type="email"
                name="email"
                required
                placeholder="sucorreo@email.com"
                class="border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#F9C74F]"
              />
            </div>

            <div class="flex flex-col">
              <label class="font-semibold text-[#0B2545] mb-2">
                Teléfono (opcional)
              </label>
              <input
                type="tel"
                name="telefono"
                placeholder="+57 300 000 0000"
                class="border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#F9C74F]"
              />
            </div>
          </div>

          <div class="flex flex-col">
            <label class="font-semibold text-[#0B2545] mb-2">
              Cuénteme acerca su proyecto
            </label>
            <textarea
              name="mensaje"
              rows="5"
              required
              placeholder="Explique qué necesita o qué idea tiene en mente..."
              class="border border-gray-300 rounded-lg px-4 py-3 focus:ring-2 focus:ring-[#F9C74F] resize-none"
            ></textarea>
          </div>

          <!-- Hidden extras -->
          <input type="hidden" name="_subject" value="Nuevo contacto desde Arte & Yeso" />
          <input type="hidden" name="_language" value="es" />

          <div class="text-center md:text-right">
            <button
              type="submit"
              class="bg-[#F9C74F] text-[#0B2545] hover:bg-[#f1b500] font-bold px-14 py-3 rounded-full transition"
            >
              Enviar mensaje
            </button>
          </div>
        </form>
      </div>
    </div>
  </section>
</Layout>
